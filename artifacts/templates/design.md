<!-- Starting point for a `design` artifact (and for its subkinds, unless one has its own template).
     Section names match `artifacts/kinds/design.yaml`; keep them if a profile requires those sections.
     Delete the italic guidance as you fill each one. -->

# Design: <name>

## Context

*The specification or problem this answers, and why it is being answered now.*

## Goals

*What this design must achieve, in outcome terms.*

## Non-Goals

*What it deliberately does not attempt — the boundary that stops scope creep later.*

## Current State

*How the system behaves today, including the parts that are wrong.*

## Proposed Design

*The intended shape: components, responsibilities, and the flow through them.*

## Interfaces

*APIs, events, CLI surfaces and contracts introduced or changed, with compatibility impact.*

## Data Model

*Entities, ownership, keys and lifetimes; what is added, changed or removed.*

## Failure Modes

*What breaks, how it is detected, and what the system does about it.*

## Security

*Trust boundaries crossed, authorisation decisions, secrets, and what an attacker gains if a
component is compromised.*

## Observability

*The signals that will show this working or failing in production, before a user reports it.*

## Migration

*How existing data and callers move, and how the system behaves while both shapes coexist.*

## Rollout

*Order of deployment, flags, and what is verified at each step.*

## Rollback

*How to get back, and the point after which you cannot — say so explicitly if there is one.*

## Open Questions

*What is still undecided, each with who decides and by when.*
