<!-- Starting point for a `test-plan` artifact. There is no `artifacts/kinds/test-plan.yaml` yet, so
     these sections are a suggestion rather than a declared expectation. -->

# Test plan: <name>

## Subject

*What is being verified — the specification, design or story this plan covers.*

## Claims Under Test

*Each behaviour to be established, phrased so a result can contradict it.*

## Levels and Coverage

*Which suites carry which claims: unit, integration, contract, property, regression, e2e.*

## Evidence Produced

*The evidence kinds this plan yields, and which verifier produces each.*

## Environment and Data

*Where the tests run and what data they need, including anything that must not be real.*

## Out of Scope

*Claims this plan does not attempt, so nobody reads a green run as broader than it is.*

## Exit Criteria

*The result that counts as verified, including the failure counts that must be zero.*
