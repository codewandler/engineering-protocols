<!-- Starting point for an `architecture-decision-record` artifact. Section names match
     `artifacts/kinds/architecture-decision-record.yaml`. One decision per record. An accepted ADR is
     never edited to reflect a change of mind — write a new one carrying `supersedes:` instead. -->

# ADR <number>: <decision, as a statement>

## Status

*One of `proposed`, `accepted`, `rejected`, `superseded` — matching
`artifacts/lifecycles/architecture-decision-record.yaml`. If superseded, link the successor.*

## Context

*The forces in play at the time: constraints, deadlines, what was known and what was not. Write it so
it still makes sense to someone who arrives two years later without the surrounding conversation.*

## Decision

*What was decided, in the active voice and in one or two sentences.*

## Alternatives

*Each option seriously considered, and why it lost. Without this the record cannot stop someone
re-proposing a rejected idea.*

## Consequences

*What this makes easy, what it makes hard, and what it commits the team to maintaining.*
