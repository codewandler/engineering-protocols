<!--
generated from billing v3
model digest 120640924b8bdbf5
compiler 0.1.0 · generator 0.1.0
do not edit: regenerate with `protocol ess generate`
-->

# Topology

Runtime requirements, stated semantically. None of this is a deployment and nothing generates a manifest from it: a replica floor of two is a claim that the system is not correct with one instance, which is a fact about the design and survives every change of hosting.

## `email-service`

Sends what other contexts ask it to send.

At least 2 instances. That is a statement about correctness, not about load — the specification says this system is not correct with fewer. No ceiling is declared.

Stateless: an instance holds nothing that outlives a request, so instances are interchangeable.

It requires:

- `publish` — `email-events`

## `invoice-service`

Issues invoices and tracks payment.

At least 2 instances. That is a statement about correctness, not about load — the specification says this system is not correct with fewer. No ceiling is declared.

Stateless: an instance holds nothing that outlives a request, so instances are interchangeable.

It requires:

- `postgres` — `invoice-store`
- `publish` — `invoice-events`

[Back to the index](README.md).

---

Generated from billing v3 · model digest `120640924b8bdbf5` · compiler 0.1.0 · generator 0.1.0. Do not edit this file; change the specification and regenerate it with `protocol ess generate`.
