# The specification's Deliverables table, as a file a check can read.
#
# `specification:agent-charter-eval-cases` § Requirements → Deliverables. "Names are the
# specification's. A later state may move one only by recording why." — so this list is the
# authority a check compares against, and a moved path is a red row until this file moves with it.
#
# Paths are relative to `integrations/claude-code/`. One per line; `#` comments and blanks ignored.

eval/run-agents.sh
eval/prompt-decomposer.md
eval/prompt-plan-reviewer.md
eval/expectations.decomposer.trace.yaml
eval/expectations.plan-reviewer.trace.yaml
eval/fixtures
eval/README.md
