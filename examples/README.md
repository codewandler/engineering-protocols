# Examples

Worked examples of complete inputs: a task, its artifact manifest, the evidence a harness would
submit, and the decisions the engine returns. These double as integration-test fixtures, so they are
kept runnable rather than illustrative.
